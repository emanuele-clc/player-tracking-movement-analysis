# Raw video / calibration - large + license-restricted, never commit
data/raw/
*.mp4
*.avi
*.mov

# ...except the small, compressed preview clips embedded in the dashboard
!docs/assets/*.mp4

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Model weights (large; regenerate via src/ scripts, or track via releases instead of git)
*.pt
*.onnx

# OS
.DS_Store
Thumbs.db
