"""
Stage 6: export all analysis outputs (tracking sample, clustering results,
heatmap grids, space-creation scores) into the JSON files docs/index.html
loads, mirroring the single-self-contained-file dashboard approach used in
expected-goals-xg-model.

Not yet implemented — see README.md pipeline table for status.
"""


def main():
    raise NotImplementedError(
        "Dashboard data export not yet built."
    )


if __name__ == "__main__":
    main()
